import React from 'react';

/**
 * Componentes stub generados automáticamente
 */
export const api = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component api">
      {children || 'api stub'}
    </div>
  );
};
