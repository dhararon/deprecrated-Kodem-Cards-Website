import React from 'react';

/**
 * Componentes stub generados automáticamente
 */
export const Pagination = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component pagination">
      {children || 'Pagination stub'}
    </div>
  );
};

export const PaginationContent = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component paginationcontent">
      {children || 'PaginationContent stub'}
    </div>
  );
};

export const PaginationItem = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component paginationitem">
      {children || 'PaginationItem stub'}
    </div>
  );
};

export const PaginationPrevious = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component paginationprevious">
      {children || 'PaginationPrevious stub'}
    </div>
  );
};

export const PaginationNext = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component paginationnext">
      {children || 'PaginationNext stub'}
    </div>
  );
};

export const PaginationLink = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component paginationlink">
      {children || 'PaginationLink stub'}
    </div>
  );
};
