import React from 'react';

/**
 * Componente stub generado automáticamente
 */
const CardGallery = ({ children, ...props }) => {
  return (
    <div {...props} className="stub-component">
      {children || 'Componente stub'}
    </div>
  );
};

export default CardGallery;
